packages:
  # Current generation only — the untracked example-*/storybook/style-package
  # dirs belong to an abandoned earlier setup and break workspace resolution.
  - "packages/themes"
  - "packages/react"
  - "packages/styles"
  - "packages/vue"
  - "packages/svelte"
  - "apps/playground"
  - "apps/demo"

onlyBuiltDependencies:
  - esbuild

overrides:
  # Dependabot alert #1: dev-server arbitrary file read, patched in 0.28.1
  esbuild: ">=0.28.1"
